:80 {
    # Only Jellyfin is exposed publicly via Tailscale Funnel
    handle /jellyfin* {
        reverse_proxy localhost:8096
    }

    # Block everything else from public access
    handle {
        respond 403
    }
}
